<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xav.sunshinecommunity.system.mapper.SysMenuMapper">


    <!--查询单个-->
    <select id="selectMenuPermissionByUserId" parameterType="long" resultType="String">
        SELECT
            distinct sm.perms
        from sys_menu sm
                 LEFT JOIN sys_role_menu srm on sm.menu_id = srm.menu_id
                 LEFT JOIN sys_user_role sur on srm.role_id = sur.role_id
                 Left JOIN sys_role sr on sr.role_id = sur.role_id
        WHERE sm.status = '0' AND sr.status = '0' AND sur.user_id = #{userId}

    </select>


</mapper>

